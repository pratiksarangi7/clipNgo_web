import 'package:flutter/material.dart';

class EmpDetailsScreen extends StatelessWidget {
  const EmpDetailsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Employee Details"),
    );
  }
}
